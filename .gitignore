# Dependencies + build artifacts
**/node_modules
**/dist
**/.vite

# OS / editor
**/.DS_Store
.vscode
.idea

# Local-only files (everything under .local/ stays on disk)
.local/
*.local.md
*.local

# Frontend env overrides
web/.env
web/.env.local
web/.env.*.local

# Cloudflare Worker local state
worker/.wrangler/
worker/.dev.vars
worker/.dev.vars.*

# Claude local state (keep .claude/settings.json shared)
.claude/settings.local.json
.claude/.session
.claude/projects/
